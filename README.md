# Bank_Dependency_injection
using Microsoft.Extensions.DependencyInjection, I was able to use the constructor injector design across the whole project. This enforced the Dependency inversion principle.
